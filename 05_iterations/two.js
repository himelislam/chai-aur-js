// while loop

// concept are the same with 1: initial variable as integer 2: condition 3: incrementing the initial value

let index = 0;

while (index <= 20) {
    // console.log(`value: ${index}`)
    index = index + 2;
}


let heros = ["Flash", "Batman", "Superman", "Ironman"]

let i = 0;

while (i < heros.length) {
    // console.log(`Names: ${heros[i]}`);
    i++
}

let score = 1;

do {
    console.log("score: ", score);
    score ++
} while (score < 10);

