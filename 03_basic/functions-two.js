function calculateCartPrice(val1, val2, ...num1){
    return num1;
}

// console.log(calculateCartPrice(122,33,1,22,322));

const user = {
    name: "hiii",
    price:23456
}

function handleObject(user){
    return `name = ${user.name} and price = ${user.price}`
}

// console.log(handleObject(user));

const myNewArray = [12,23,34,45,56,75];

function retureSecondValue(getArray){
    return getArray[1];
}

console.log(retureSecondValue(myNewArray));